# Site assets

Replace these placeholders with real images. The page references:

| File | Used for | Suggested size |
|------|----------|----------------|
| `screenshot-flash.png` | "Flashing a CF card" tile | 800×600 (4:3) |
| `screenshot-add.png`   | "Loading an album" tile  | 800×600 (4:3) |
| `screenshot-ipod.png`  | "The finished iPod" tile | 800×600 (4:3) |
| `og-image.png`         | Social/link preview      | 1200×630 |
| `favicon.svg`          | Browser tab icon         | (already provided) |

Until you add the three screenshot PNGs, those tiles will show broken-image
icons. Drop in real captures (a terminal flashing a card, an `add` run, a photo
of the finished iPod) and the page picks them up automatically — no code change
needed beyond matching the filenames above.
